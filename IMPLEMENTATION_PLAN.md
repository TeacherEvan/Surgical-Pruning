# Implementation Plan — Surgical-Pruning

> **Status: COMPLETE (verified 2026-09-15).**
>
> This root-level index previously pointed at `docs/GAP_PLAN.md` (the gap
> register) and described a phased remediation. Both the gap register and the
> phased plan are now **archived** as verified-complete snapshots:
>
> - `docs/plans/.archive/2026-09-15-GAP_PLAN.md` — full gap register (A1–G1)
> - `docs/plans/.archive/2026-09-15-IMPLEMENTATION_PLAN.md` — phased remediation
>
> **Verification:** `pnpm run lint && typecheck && test && build` all green
> (11 packages, 16 test files). Every gap item was confirmed implemented in the
> live tree before archiving; no re-derivation was performed.
>
> Canonical planning for future work lives under `docs/plans/`.
