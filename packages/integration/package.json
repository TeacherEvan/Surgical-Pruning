{
  "name": "@surgical-pruning/integration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@surgical-pruning/core": "workspace:*",
    "@surgical-pruning/reviewer": "workspace:*",
    "@surgical-pruning/planner": "workspace:*",
    "@surgical-pruning/executor": "workspace:*",
    "execa": "^10.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
